<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MillingApp.resoultPage">
    <ContentPage.Content >
        <ScrollView>
            <StackLayout BackgroundColor="{StaticResource blackBackground}">
        <Label Text="Milling Parameter" 
                   TextColor="{StaticResource goldColor}"
                   HorizontalTextAlignment="Center"
                   FontSize="Title"
                   Padding="12, 12, 0, 20"
                   TextTransform="Uppercase"
                   TextDecorations="None">
        </Label>
                <Grid>
                    <Label Style="{StaticResource labelTextStyle}" Text="VC = "></Label>
                    <Label x:Name="resoultVc" Text="" Style="{StaticResource labelStyle}" Grid.Column="1" Grid.Row="0"></Label>
                </Grid>
        </StackLayout>
        </ScrollView>

    </ContentPage.Content>
</ContentPage>